# React + TypeScript + Vite – Starter Template

A clean starter template built with **React**, **TypeScript**, and **Vite**.
Use this as a base for new projects without re-configuring everything.

## 🚀 Getting Started

1. Click **Use this template**
2. Create a new repository
3. Clone it locally
4. Install dependencies:

   ```bash
   npm install
